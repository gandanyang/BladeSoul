# T49 交接（蒙皮权重 / "打击感"的地基）· 2026-09-14

> 接 `T48-HANDOFF.md`。项目 `G:\Game`（Godot 4.7.1 mono + C#，Oniblade 鬼刃）。
> T48 的武器骨**没有回归**（`AnimProbe`：`Weapon_R` 全局偏转 115.4° ≥ 100°）。
> 这一轮解决的是 T48 交下来的第 1 号遗留：**自动权重炸尖刺**。

---

## 一、最重要的一条：找到并修掉了一个真 bug（与骨骼无关）

`src/Player/HumanoidAnimator.cs:248`

```csharp
ApplyAttackPose(attackFrame / Utils.Frames.PerSecond);   // ← 错
ApplyAttackPose(Utils.Frames.ToDelta(attackFrame));      // ← 对
```

`PerSecond` 是 `int` 常量，`int / int` 走**整数除法**，`attackFrame` 在 0..59 之间时结果**恒为 0**。
于是 `ApplyAttackPose` 里 `t` 恒为 0 → `strike` 恒为 0 → `strikeEase` 恒为 0。

**后果：三连斩从头到尾停在"起手"那一拍。** 玩家看到的是"举着刀不动、然后瞬间切回收招"，
根本没有出刀过程。这是"完全没有打击感"里**与骨骼、与权重都无关**的那一半。

证据（修复前，逐帧读 `GetBonePoseRotation`）：

```
第 0 帧：当前 133.7° / 起手 133.7° / 出刀 82.3°
第 8 帧：当前 133.7° / 起手 133.7° / 出刀 82.3°
第16 帧：当前 133.7° / 起手 133.7° / 出刀 82.3°
```

修复后第 16 帧变成 `82.3°`（= 出刀姿势），三段各自走出自己的刀路。

`tools/check.ps1` 全量通过（34 步 / 197 项单测），说明这个 bug 之前没有任何测试覆盖到。

---

## 二、权重问题的真实样子（扫描出来的事实，不是推测）

**先说结论：原始权重只有一个缺陷。**

用 `tools/scanbad.py` 的思路把原始权重整个扫了一遍：

```
主导骨 = neutral_bone 的孤儿顶点 : 1265 个 (11.4%)   ← 唯一的缺陷
主导骨离顶点 > 0.45 m            : 0 个
左右侧被对侧骨主导                : 0 个
```

那 1265 个孤儿顶点的范围很明确：`y -0.979..0.484`、`|x| 0.285..0.531`，
按材质是 `ClothOuter` / `Leather` / `Belt` —— **身体两侧悬垂的袍子与甲片**。
Blender 的骨热权重只在这块远离骨架的几何上失败了，其余地方是健全的。

### 所以正确的修法是"只动这 1265 个"

`tools/weight_fix.py` 就是干这个的：只重算那 1265 个顶点，剩下 9806 个顶点
**一个字节都不碰**。修复规则见脚本头部注释，要点是
**距离闸门 + 左右隔离 + 反向关节兜底**。

### 一次走错的路（务必别重犯）

中途我曾用一个"全量重算"的脚本（已删，别再造）**把全部 11071 个顶点按几何重算了一遍**。
数字上它更好看（棱伸长 1754 mm → 124 mm），于是我把原始权重覆盖掉了。
**结果人物在游戏里明显变形**——拿好的权重去换坏的，是最典型的自伤。

教训：**"指标变好"不等于"变好了"。** 这个项目的验收习惯是"能拿数据自证"，
但数据自证之前，先要证明**数据本身指向的东西是对的**。

### 三版对照（同一套探针、同一套动画，全部实测）

| 指标 | 原始 | 全重算（肇事版，已弃） | **只修孤儿（当前）** |
|---|---|---|---|
| 被 `neutral_bone` 冻住的顶点 | 1265 | 0 | **0** |
| 权重被改动的顶点数 | — | 11071（全部） | **1265** |
| 静止 棱伸长峰值 | 1754 mm | 124 mm | 456 mm |
| 轻斩·叁 棱伸长峰值 | 1754 mm | 140 mm | 450 mm |
| 走路 棱伸长峰值 | 1754 mm | 179 mm | 506 mm |
| 偏离主导骨峰值（各动作） | 0.308 m | 0.267–0.403 m | 0.308 m |

注意最后一行：**"只修孤儿"的偏离主导骨与原始逐位相同（0.308 m）**。
原因是那 1265 个孤儿顶点只被 `neutral_bone` 一根骨影响，**没有非主导骨可偏离**，
所以这个指标**看不见孤儿顶点的病**——它量的是"权重粘错骨"，量不到
"权重粘在一根永不动弹的骨上"。别把"指标没变差"读成"没修好"：
这两版的真实差别是棱伸长 1754 mm → 456 mm，那些顶点从"完全不动"变成"跟着身体走"。

数字只作参考。**真正的判据是渲染出来的图**——见 `scenes/tests/PoseShot.tscn`，
它一次出正面/背面/左/右四张。

### 关于"头朝下"：没能复现，而且两版都对

用逐像素轮廓分析（不靠肉眼）量过三个版本，**都是下宽上窄的正常人形**：

```
原始权重      上半段平均宽 217.9   下半段 265.2
全重算        上半段平均宽 216.6   下半段 286.3
只修孤儿      上半段平均宽 207.1   下半段 279.4
```

倒立的话轮廓必须是**上宽下窄**，三个都不是。所以那几次"头朝下"的判断是
**渲染视角造成的误判**（相机在 y=1.05 俯视一个 y=0.1 的角色，参考线又画在地上）。
这也是为什么本轮新增了 `PoseShot` —— **朝向这种事必须固定机位出图，不能靠描述**。
## 三、判据是怎么定下来的（这部分比结论更值钱）

设计判据时踩了四个坑，每一个都会产出**看起来很像真的假数据**：

1. **`Skeleton3D.GetBoneGlobalPose()` 在无头场景里不刷新。**
   姿势已经从起手转到出刀，它逐帧返回一字不差的同一个值 → "顶点位移"永远算出
   `0.000 m`。**一个永远绿的探针比没有探针更糟。**
   现在的 `WeightProbeTest` 完全不碰骨架空间 API，只从 `HumanoidAnimator` 读旋转、
   自己构造旋转矩阵、自己算顶点位置。

2. **`GetBonePose(i)` 返回的已经是完整局部变换**（静止时它逐位等于 `GetBoneRest`）。
   再乘一次 `rest` 等于把骨骼链算两遍 → 误差 2.01 m。

3. **`Skin.GetBindBone()` 对本资产每个绑定都返回 -1**（`GetBindCount()`=24）。
   拿它映射下标 → 顶点位置恒为 `(0,0,0)` → 又是一个假绿探针。

4. **`SetBonePoseRotation(b, Identity)` 不等于"骨骼回到静止"。**
   本模型 `L_Toe` 的 rest 基是 `diag(-1, ·, ·)`（自带 180° 翻转），
   Godot 会在下一次骨架刷新时把"相对旋转"算回 180°，**基准姿势整体偏掉 0.30 m**。
   所以基准帧只能用"动画器自己设过的姿势"。

还有两条关于指标本身的：

* **单帧位移不能当主判据**：快速挥腿时腿部顶点一帧移动 0.4 m 是**物理正确**的
  （角速度 × 半径）。它会把正常动作报成缺陷。
* **棱长比在短棱上会爆**：0.009 m 的棱"伸长 951%"其实只有 9 mm。
  要用**绝对伸长量**。

现在的主判据是 **偏离主导骨（`PivotDrift`）**：顶点位置与"只被主导骨刚性带动时应在的位置"
之间的距离。它对基准帧、对动画快慢都免疫，物理含义直白——主导骨决定这个顶点"长在哪"，
其余骨骼把它从那儿扯开。

---

## 四、复现 / 继续

```powershell
# 体检（权威，在引擎里）
godot --headless --path . res://scenes/tests/WeightProbe.tscn
godot --headless --path . res://scenes/tests/AnimProbe.tscn
godot --headless --path . res://scenes/tests/PoseProbe.tscn
godot --path . res://scenes/tests/PoseShot.tscn          # ★ 出四方向截图（要窗口）

# 修权重（几何不动，只改那 1265 个顶点的 JOINTS_0 / WEIGHTS_0）
python tools/weight_fix.py --src %TEMP%\original.glb --out %TEMP%\fixed.glb
copy %TEMP%\fixed.glb assets\models\model_player_congyun_03_textured.glb
godot --headless --path . --import
```

细节、以及四个**会让权重复算悄悄错掉**的陷阱，写在 **`tools/README-weights.md`**。

## 五、还没做完

1. **视觉验收只做了一半。** `PoseShot` 能出静止站姿四视图，但**还没做攻击连帧和走路的**
   对比渲染。下一步应该照着 T48 的 `T48SwingShot` 出连帧，用眼睛确认挥刀时那 1265 个
   袍子顶点（原来的"破碎飞散"）是不是真的跟着身体走了。
2. **`weight_fix.py` 的参数没有扫描过。** `GATE=0.30` / `FALLOFF=3.0` / `SEG_EPS=0.03`
   都是第一版取值，只有 `GATE` 的"绝对上限"变体被证伪过。袍摆顶点到最近骨 0.35 m，
   这个量级决定了它们跟着腿还是跟着腰，值得单独调。
3. **`assets/models/model_player_congyun_03_textured.glb` 不在 git 里（untracked），
   没有备份。** 这一轮我一度把它整个覆盖掉，"修复前"的状态是靠
   `tools/restore_original_weights.py` 从 `_rigged_weapon.glb` 按顶点位置搬回来才恢复的
   （11071/11071 匹配，最差位置误差 3.6e-07 m，孤儿数 1265 对得上）。
   **动它之前先复制到 `%TEMP%`。**
4. 模型里那个 42 顶点的单位球（把包围盒撑到 ±1）还在，`AnimProbe` 报的
   minY -0.820 就是这个原因。脚底与胶囊底面差 -0.145 m，`AnimProbe` 判为可接受。