# T51 交接 · 格挡与弹开：测量结论与两项改动

## 一句话

**弹开机制完全正确，窗口宽度精确等于配置值。**
玩家"从来弹不开"的原因是**时序 + 看不见**：窗口只在**按下右键那一刻**开一次，
而敌人前摇 24 帧 > 旧窗口 9 帧，两者精确错开——按住右键等刀来必然错过。

本轮做了两件事对症下药：**窗口延长到 15~20 帧** + **弹开窗可见指示器**。

## 一、改动一：窗口延长 6/9/12/16 → 8/15/17/20

`data/difficulty/*.tres` + `docs/02-战斗系统设计.md` §2.2 表格**同步改**
（铁律：每个数字只有一个来源，改数据不动文档就是埋雷）。

| 难度 | 旧 | 新 | 时间 |
|---|---|---|---|
| 修罗 | 6 | **8** | 133ms |
| 武士（默认） | 9 | **15** | 250ms |
| 剑客 | 12 | **17** | 283ms |
| 见習 | 16 | **20** | 333ms |

上限受 `CombatTuning.MaxDeflectWindowFrames = 20` 硬约束（未改）。

**实测验证**（`GuardWindowProbe`）：

```
[窗口扫描] 难度档（武士）配置的弹开窗 = 15 帧
[窗口扫描] 点按有效区间 = 提前 0~14 帧（15 帧宽）      ← 改前是 0~8（9 帧）
```

单测**不会**因此变红：`CombatTuningTests` 里的 9 是参数化测试数据
（`ResolveDeflectWindowFrames(9, 1, 2)`），不是难度值。

**没有破坏防连打**：`DeflectTraining` 的连打模式仍然是
`弹开 0 / 格挡 3 / 挨打 1`——窗口变宽没有让连打成为可行策略。

## 二、改动二：弹开窗指示器（`data/player/deflect_cue.tres`）

### 机制先明确（制作人口述 → 已写进 `docs/02` §2.2）

**弹开窗 = 按下防御键那一刻打开，持续 D 帧。它不是"按住期间一直有效"。**

| 操作 | 窗口 | 被命中时 |
|---|---|---|
| 在敌人出招前 **0~D 帧内按下** | 开着 | **弹开**，体干 **0** |
| **提前按住不放**（窗口已过期） | 关着 | **格挡**，扣体干 **20~45** |

两刀就能把架势槽（上限 100）打空。早按的代价是**真实的**，不是手感问题——
`CombatResolver` 第 5 条：`Block` 走 `atk.Traits.PostureDamage`（敌人招式 20~45），
而 `Deflect` 的只给敌人 +18、自己 0。

### 指示器：一盏静态的灯

- `src/Player/DeflectCueProfile.cs`（新）+ `data/player/deflect_cue.tres`（新）
- `src/Player/PlayerActor.cs` —— `BuildDeflectCue()` + `UpdateDeflectCue()`
- `scenes/actors/Player.tscn` —— 挂 `DeflectCue`

**它只回答一个是非题：「现在按下去算不算弹开」。**
亮 = 能弹开，灭 = 只剩格挡。**不渐变、不脉冲、不自转。**

三轮动效都是被**实测**否掉的，不是审美取舍：

1. 第一版做了"越接近关闭越亮"——**方向是反的**。光环亮着本身就意味着
   "现在按下去能弹开"，再强调"快没了"只会读成"该按了"，而它恰恰马上要熄灭。
2. 第二版留了自转当"活气"，理由是"圆形各向同性、转不出信息"。实测否掉：
   相邻帧 0.61% 像素在变、最大单像素差 197，全落在环的区域。
3. 第三版"每帧写同样的颜色"——逻辑等价，**实测不等价**：每帧赋
   `AlbedoColor`/`Emission` 会不断触发材质属性更新，环仍在闪。
   改成**开窗那一刻只写一次**。

**最终实测**（同一窗口内三帧，按真实剩余帧数抓）：

```
pose_cue        蓝像素 2913  质心 (508.8, 482.2)  亮度均 230.8
pose_cue_late   蓝像素 2928  质心 (509.2, 481.9)  亮度均 230.9
pose_cue_late2  蓝像素 2928  质心 (509.2, 481.9)  亮度均 230.9

相邻两帧差异 = 9 像素 = 0.0012%      （带自转/每帧写材质那版是 1.5683%）
```

环的像素数、质心、亮度三项逐帧一致 —— 它确实是一盏不动的灯。

**对判定零影响**（纯表现层，`CastShadow = Off`，不参与碰撞）。

## 三、测量工具：`scenes/tests/GuardWindowProbe.tscn`

`DeflectTraining` 只测一个点（提前 6 帧按），它证明"窗口是通的"，
但证明不了**窗口有多宽**，更看不见"按住为什么会失败"。
这是一个关于**窗口形状**的问题，单点测试原理上就看不见。

新探针枚举两种真实的按键习惯，扫出成功/失败的分界，并打印
**窗口内部帧号序列**（`GuardState._window.FramesSinceEntry` 与
`DeflectWindowFramesLeft`），让"窗口到底开没开"可以直接看见。

`MaxLeadFrames` 要在 `.tscn` 里配（窗口变宽后要跟着加大，否则扫不到边界）。

## 四、格挡姿态"进入了但不明显"——量化了

用**同一台游戏相机**（`GameViewPoses.tscn`）拍三个姿态做像素差异：

| 姿态 | 与站桩的差异像素 |
|---|---|
| 格挡（改动前） | 886 / 186624 = **0.47%** |
| 格挡（改动后） | 1916 / 186624 = **1.03%** |
| 窗口开着（含指示器） | **1.52%** |
| 出刀（对照） | 20276 / 186624 = **10.86%** |

**根因**：原 `ApplyGuardShape` 只抬手臂，而相机在角色**背后**，
手臂正好被躯干和头挡住，差异只集中在 98×171 像素的小块里。

改动加了"背后视角也看得见"的三件事：重心下沉（腿弯 0.72/1.0 rad）、
侧身（骨盆 −0.46）、双手抬高前伸。

**仍未解决**：1.03% 远低于出刀的 10.86%。只调骨骼幅度收益递减
（0.47→0.82→1.03，幅度每次都翻倍却只换来 0.2 个点）。
下一步若还要更明显，应该走**非骨骼反馈**（脚下光环已经是一个例子）。

## 五、顺带查出的两条死路

| 东西 | 状态 |
|---|---|
| `DeflectChainEvent` / `DeflectChainChanged` | 事件被 `Raise`，但**零订阅者**——弹开连击没有任何 UI 反馈 |
| `InputMap` 的 `lock_on` 动作 | 绑了鼠标中键，但**代码里零处理** |

## 六、剩下可做的（按价值）

1. **接上 `DeflectChainChanged`**，连击数进 HUD——事件已经在了，只差订阅者。
2. **弹开成功的显眼反馈**（火花/白闪/顿帧）。现在只有 `ApplyDeflectPose` 的骨骼震动 + 音效。
3. 真机试玩验收：M1 的"弹开成功时会想再试一次"**至今没做过正式验收**（见 T44）。

## 七、探针自己踩的坑（每条都会给出错误结论）

1. **固定帧间隔按键**：`FramesPerSample=80` 短于假人 126 帧的出招间隔，
   那一轮假人根本没出招，却报成"按住时敌人不打你"。
2. **模式之间不等过 `GuardReentryLockFrames`**：上一轮结尾的松手会让下一轮
   被判成"快速重按"，于是"按住"整轮零弹开，看起来像系统 bug。
3. **按住模式一开始就按住**：等到敌人起手时窗口早过期了，
   测到的只是"过期后的状态"，而不是"按住能不能弹开"。

## 八、本轮改动的文件

- `data/difficulty/{asura,samurai,kenshi,migoto}.tres` —— 窗口延长
- `docs/02-战斗系统设计.md` §2.2 —— D 值表同步
- `src/Player/DeflectCueProfile.cs`（新）+ `data/player/deflect_cue.tres`（新）
- `src/Player/PlayerActor.cs` —— 指示器挂载与每帧同步
- `scenes/actors/Player.tscn` —— 挂 `DeflectCue`
- `src/Player/HumanoidAnimator.cs` —— `ApplyGuardShape` 加强
- `src/Dev/SelfTest.cs` —— `data/player/` 允许 `DeflectCueProfile`
- `src/Dev/GuardWindowProbeTest.cs` + `scenes/tests/GuardWindowProbe.tscn`（新）
- `src/Dev/GameViewTest.cs` + `scenes/tests/GameViewPoses.tscn` —— 四姿态连拍

---

# ✅ 遗留① 已完成（2026-09-14，接手的 agent）

> **「接上 `DeflectChainChanged`，连击数进 HUD」这条做完了**，见上一节 §6 的第 1 条。

## 一、做了什么

| 文件 | 改动 |
|---|---|
| `src/Combat/CombatActor.cs` | 连击的**唯一写入点** `SetDeflectChain()`；三处变化（弹开 +1 / 窗口超时 / 复活复位）全走它，**每次都往 EventBus 发事件**（带 ActorId）。窗口 `90` 提成 `[Export] DeflectChainWindowFrames`（原来硬编码在 Deflect 分支里） |
| `src/Audio/AudioDirector.cs` | 不再自己 `++`、不再自己发事件，改成**订阅** `DeflectChainChanged`（音高与屏显从此同源）；拼刀/破防/一闪/挨打时**立刻**回落到基准音高这条保留 |
| `src/UI/Hud.cs` | 新的「×n」标签：`≥2` 才显示、字号随连击加大、断连后淡出 36 帧。数据走**轮询** `ICombatActorDebug.DeflectChain`（与血/体干同一条路，见 Hud 类注释） |
| `src/Dev/HudTest.cs` | 新增第 6 组自检（走真链路，见下） |
| `src/Dev/DeflectChainShot.cs` + `scenes/tests/DeflectChainShot.tscn` | **新**：真机连帧截图探针（带窗口跑） |
| `assets/references/t51_chain_1..3.png` | 验收截图 |

## 二、修掉的那个缺陷长什么样

`DeflectChainEvent` 以前**只有一个生产者（AudioDirector）和零个订阅者**，而且：

- `ActorId` **从来没被填过**（恒 0）——字段在事件里，但没人知道是谁弹的；
- **断连那一半从来不发**（`ResetDeflectChain()` 只改自己的字段）——
  于是订阅者眼里的连击**只增不减**。这正是 T50 那类"数值全对但语义是死的"同一个病。

现在：生产者唯一（战斗层）、消费者真实存在（音频）、屏显读权威值（HUD 轮询）。

## 三、★ 顺手量出来的一个**设计事实**（这条比改动本身更值得看）

**对着一个靶子，连击永远连不上第 2 次。**

| 量 | 值 |
|---|---|
| 假人两次**发动**之间的间隔 | 58（招式）+ 60（冷却）= **≈118 帧** |
| 连击保持窗口 | **90 帧** |

118 > 90 → 第二刀落下时窗口早关了。所以 `DeflectChain`（以及**依赖它的弹开音高递增**，02 §7）
在单挑里恒为 1：**音高递增那套设计，只有多敌人才听得到**。
自检里因此放了**两个相位错开 60 帧的靶子**——这不是为了凑测试，是"连击"这个概念的前提。

> 要不要把窗口放大过 118（让单挑也能连）是**平衡决策，留给制作人**；
> 我没有偷偷改这个数，只是把 90 提成了可在场景/数据里改的 `[Export]`。

## 四、证据（可复现）

```powershell
godot --headless --path . res://scenes/tests/Hud.tscn          # 断言：×2 显示 / 断连归零 / 淡出
godot --path . res://scenes/tests/DeflectChainShot.tscn        # ★ 必须带窗口：连帧截图
powershell -NoProfile -File tools\check.ps1                    # 34 步全绿
```

HudTest 的实际输出：

```
[HUD] 弹开连击：连上 ×2 → 屏显 ×2（第 1 次时未显示 ✓）
[HUD] 断连：静置 96 帧 → 战斗链 0 / 屏显 ×0 ✓
[HUD] 淡出：36 帧后连击数已隐藏 ✓
```

截图探针的实际输出（**抓图时机是事件驱动的**，不是按固定帧数——固定帧数在"这次没连上"时会
拍到空图，反而什么都证明不了）：

```
[T51连击图] 连上 ×2 → res://assets/references/t51_chain_1.png（屏显 ×2，战斗链 2，第 212 逻辑帧）
[T51连击图] 后（约 30 帧后仍在） → res://assets/references/t51_chain_2.png（屏显 ×2，战斗链 2，第 242 逻辑帧）
[T51连击图] 断连+淡出后（应无 ×n） → res://assets/references/t51_chain_3.png（屏显 ×0，战斗链 0，第 375 逻辑帧）
```

**截图自己修掉了两个只有看图才能发现的问题**（数值断言全过）：

1. 连击数原本放在屏幕底部 −222，**正好压住弹开光环的上沿**，`×2` 被切掉一块 → 移到 −256；
2. 字号 34 比「弹开」提示（26）**还不显眼**，而连击是更重要的信息 → 提到 40（步进 3→4）。

> 这就是 AGENTS.md 那句"编译过不等于做完了"：**HUD 的读数是对的，但屏幕上读不出来**，
> 只有连帧截图能证伪。

## 五、还没做的（按价值）

1. **弹开成功的显眼反馈**（§6 第 2 条，未做）：现在只有骨骼震动 + 音效 + 边缘脉冲（18 帧、0.55 透明度）
   + 脚下光环。真正缺的是**带方向的火花**——`HitEvent` 里已经有 `Position` 与 `Direction`
   （T28 特意加的），但 `CombatVfx` 的弹开火花是不是用了 `Direction` 值得查一遍。
2. **`lock_on` 动作零处理**（§5，未做）：InputMap 绑了鼠标中键、代码里没有消费者。
3. **真机验收 T44**：M1 的唯一标准仍然没人回答过。**连击数接上之后，第 4 步
   （"成功弹开三次 → 想不想再来一次"）才有得看**——现在屏幕上第一次有了"我连上了"的读数。
4. `AnimProbe` / `MoveProbe` 的阈值重校准（T50 §"还没做完"第 1 条）**仍未做**：
   实测武器骨全局偏转 132.6°（阈值 100°），两腿夹角差那一条也得重新量。
   这两条都**需要人眼判"够不够"**，不适合 agent 自己改数。
