# 2026-09-13 收工交接单

> **用途**：明天（或下一个人）从这一页接上。**不要重放对话历史。**
> 快照时刻：2026-09-13 深夜 ｜ `HEAD = d7cf767` ｜ 工作区**干净**，本地 = 远端

---

## 一、归档时的状态

| 项 | 值 |
|---|---|
| 远端 | `git@github.com:gandanyang/BladeSoul.git`（SSH，本机已通） |
| 提交数 | 54+（今天新增十几笔，见 `git log --since="2026-09-13 00:00"`） |
| 工作区 | **干净**（所有在制品都已归档，含别的 agent 的 WIP 快照） |
| 自检 | `tools\check.ps1` → **26 步全绿 / 164 单测 / 退出码 0** |

---

## 二、今天落进库的东西

### 主线（制作人这条线）

| 主题 | 落在哪 |
|---|---|
| **T35/T39 本地 3D 生成通道** | `docs/13-本地3D生成流程.md`（五步流程，含"去碎渣→降面→**补法线**"这条原本没有的第 2.5 步）；显存峰值 **9.31GB**、单次 **77 秒** |
| **女主角「绫」的资产** | 各方位 6 张 ＋ 人设 6 张（`assets/references/ref_heroine_aya_*`）；可用模型 `assets/models/heroine_aya_v2a_clean.glb` |
| **T45 落下平台重置** | `src/World/FallRecovery.cs`（纯逻辑）＋ `src/Player/FallGuard.cs`（挂在 Player.tscn）＋ `data/player/fall_recovery.tres` |
| **体检工具 PlayerGaps** | `scenes/tests/PlayerGaps.tscn`：把"掉落保护"变成**硬断言**（三组对照全过），并把动画覆盖变成**数字** |
| **排期与流程文档** | `docs/14-下一阶段排期.md`（三条阻塞）、`docs/15-开场教学流程.md`（0:30→23 分钟时间轴） |
| **AGENTS.md 状态修正** | 从"12 步/143 单测/M1"更正为"26 步/164 单测/**M5 途中**" |

### 其他人的（zcode ＋ 另一位）

| 主题 | 状态 |
|---|---|
| **T31 战斗 HUD 收口** | ✅ 完成（`EnemyBars` 头顶条 ＋ `HudShot` 截图 ＋ 修了体干条锚点 bug） |
| **魔骸足兵上色并接入道场** | ✅ 完成（`data/materials/makugai_body.tres` ＋ `Dojo` 摆 `Makugai`） |
| **T37 架势收口 / T40 室内照明** | 🟡 **在制品**（执行者尚未报告收工）——已由制作人**代为归档**（提交 `d7cf767`），免得过夜丢东西。它手上还有一行没接：把 `PlayerGaps` 加进 `check.ps1` |

---

## 三、明天从哪开始（按优先级）

| # | 做什么 | 为什么是这个顺序 |
|---|---|---|
| 1 | **T44 · M1 真人验收**（只能由人做，5 分钟） | M1 唯一标准是"弹开成功时会想再试一次"。**条件已经齐了**（架势槽可读、音高递增、重开 ≤3 秒）。这条不成立，后面两个月全是白做 |
| 2 | **T46 · 重攻击（蓄力斩）** | 解锁教学第一幕；**开工前先对账**（见 §五） |
| 3 | **T41 · 跳跃** | 已解冻可开工（机制独立，**动画依赖 T24**） |
| 4 | **T37 / T40 收尾** | 等 zcode 报告；顺手把 `PlayerGaps` 接进 `check.ps1` |
| 5 | **两条 P0 清理**（审阅结论，未开工） | ① `check_dead_config.ps1`：288 个 `[Export]` 里有 **18 个零引用**；② 动画时长硬编码（`HumanoidAnimator` 三处）——**T38 开工前必须改** |

---

## 四、等你拍板的三件事

1. **跳跃键位**：`jump`＝空格、`dodge` 从空格改到**左 Shift**（与 `sprint` 共键）。按只狼手感定的，**有偏好说一声即可改回**（00 §六-6）
2. **「重攻击」＝蓄力斩**？（00 §六-4）若你要的是"按一下就出的单发重击"，那是**新机制**，需要单独设计
3. ~~第一幕教三连还是交互~~ —— **已消解**（03 §4.1 已对齐 §4.0）

---

## 五、已知的坑与留白（交接时最容易漏的）

1. **重攻击的数据已经和规格漂了**：`charged_*.tres` 位移 `0.8` vs 规格 `+1.5m`；`Unblockable=false` vs 备注"破防，不可防御"。
   原因**不是疏忽，是这份数据从来没人读过**——没人读就不会报错，只会悄悄和设计分家。T46 卡里写着"先对账"。
2. **跳跃解冻有连带影响**：`10 §3.4` 的"室内战斗区净高 ≥5m"是按"SpringArm 4.2m ＋ 不跳"推出来的，
   **跳跃会把机位再抬高 1~1.5m，这条得重算**（也影响 T40 的净空判断）。
3. **掉出世界 ＝ 脱离当前遭遇战**（T45 只回位置、不掉血）。要不要连遭遇战一起重置，**等 T42 定**。
4. **并发写法仍未解决**：现在 5 个 agent 直接写同一个工作区，唯一保护是"卡片里写不许动哪个文件"。
   今天已出现：重复看板行、一次**假构建失败**（重跑就绿）、两次险撞车（`Dojo.tscn` / `PlayerActor.cs`）。
   远端已经建好了 → **短分支 + 合并**能一次性消掉这类问题。
5. **`.uid` sidecar 漏过一次**（T31 那批），值得加一条自检："每个 `.cs` 必须有同名 `.uid`"。

---

## 六、三条阻塞（细看 [docs/14](14-下一阶段排期.md)）

| 阻塞 | 卡住了什么 |
|---|---|
| **A 姿势从哪来** | **一次卡住 5 张卡**：T38 动画、T25 主角、T26 笼手、T27 敌人、T29 女主 |
| **B 没有遭遇战编排** | "它还不是一个关卡"（现在只是道场沙盒）→ T42，**纯逻辑、不需要美术** |
| **C M1 验收没做过** | 后面所有投入的赌注确认 → **T44，只能由人做** |
